import Auth from "../views/auth";
import SignUpForm from "../views/auth/SignUpForm";

const SignUpPage = () => {
  return (
    <Auth>
      <SignUpForm />
    </Auth>
  );
};

export default SignUpPage;
